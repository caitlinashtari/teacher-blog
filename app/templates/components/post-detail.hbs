<div class="row">
  <div class="col s3">
    <img class="responsive-img" src="{{post.image}}" alt="{{post.description}}">
  </div>
  <div class="col s9">
    <p>HELLO{{post.content}}</p>
  </div>
</div>
<button {{action 'delete' post}}>Delete</button>
